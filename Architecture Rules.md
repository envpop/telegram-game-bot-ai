# Telegram Game Bot — Architecture Rules
## 架構慣性守則 (Version 1.1 統合版)

### 1. 核心原則
本專案採用「 **接收 → 解析 → 觀察與策略判斷 → 動作執行** 」的分層架構 [1, 2]。
*   **各層職責分離**：各層只負責自己的工作，絕不因為單一功能的開發方便而跨層處理 [1, 2]。
*   **規則是限制責任，而不是限制功能** [1]。如果一個功能需要跨越複數層才能完成，應優先調整模組邊界，而不是讓現有的模組增加例外 [1]。
*   **消除重複邏輯，而非消除重複 I/O** [3]。

---

### 2. Monitor 層 (monitor.py)：只負責接收
`monitor.py` 是 Telegram 的接收層 [2]。它的核心原則是：**Monitor 不知道遊戲規則** [2]。

#### 負責 (可以做)
*   接收 Telegram 訊息與訊息編輯事件 [1, 2]。
*   收集並整理訊息基本資訊 [2]，轉換成 `raw record` [1]。
*   寫入並記錄 `raw JSONL` [1, 2]。
*   處理媒體下載等「接收當下」的工作 [2]。
*   將 `record` 傳給後續流程（下一層）[1, 2]。

#### 不負責 (不可以做)
*   **禁止** 導入（import）`parser` 或 `executor` [1]。
*   **禁止** 判斷遊戲規則或解析遊戲語意 [1, 2]。
*   **禁止** 判斷是否要採取行動或決定執行什麼動作 [1, 2]。
*   **禁止** 呼叫 `executor` 或 `scheduler` 執行遊戲動作 [2]。
*   **禁止** 主動送出遊戲指令 [1]。

---

### 3. Parser 層：只回答「這是什麼」
Parser 負責將 `raw record` 轉換成結構化資料 [4]。其核心問題是：**「這是什麼？」**，而不是「現在應該做什麼？」 [5]。

#### 負責 (可以做)
*   判斷訊息來源與訊息類型 [4, 5]。
*   解析使用者指令與特定訊息格式 [4, 5]。
*   解析 Server Response、遊戲公告、按鈕及其他訊息結構 [5]。
*   將文字轉成結構化資料，並進行 `response shape` 判斷 [4, 5]。

#### 不負責 (不可以做)
*   **禁止** 呼叫 `executor` 或 `scheduler`（不建立 scheduler job）[4, 5]。
*   **禁止** 產生遊戲動作或決定自動化策略 [4]。
*   **禁止** 修改遊戲策略狀態，或修改遊戲狀態以外的業務資料 [4, 5]。

---

### 4. Strategy 層：負責「根據資料做決策」
Strategy 是本專案主要的決策與「理解遊戲狀態」之單位 [4, 6]。其核心問題是：**「根據目前已知的資訊，我理解到了什麼，以及應該做什麼？」** [6]。

#### 負責 (可以做)
*   **多源資料決策**：可根據 `parsed`、`record`、`JSONL 歷史資料`、`catalog`、`profile 資料`、`runtime state`、`當前遊戲狀態` 或其他 Strategy 已建立的資料來判斷是否需要採取行動 [4, 5]。
*   **解除單一訊息限制**：Strategy 不限定只能「反應當前訊息」 [4, 5]。它可以維護/同步持久化資料、建立跨訊息狀態、產生供後續判斷使用的資料，並在需要時提出 Action [6]。

#### 架構與分類規範
*   **不以「是否主動」作為硬性分類** [6, 7]：實際功能可能同時具備「立即反應訊息」、「讀取歷史資料判斷」、「建立狀態供後續使用」或「產生遊戲動作」等多重特徵 [6, 7]。
*   **不應依賴訊息來源** [8]：不要用 `chat_id`、`user_id` 作為主要架構分類，來源判定是 Parser 的責任 [8]。Strategy 應以「其理解/處理的問題」來分類 [8]。
*   **Trigger 的定位**：Trigger 應被視為一種功能概念，而非比 Strategy 更高或更低的一層 [6, 7]。如果一個 Trigger 需要複雜的內容分析、歷史資料或跨訊息狀態，應直接實作為 Strategy [6, 7]。

#### 4.1 Passive Strategy (Observation Strategy / 被動觀察型)
*   **定義**：只觀察與建立資料，更新狀態，不直接產生或主動送出遊戲動作 [7, 8]。
*   **實作**：實作 `observe(parsed, record)` 標準介面 [8, 9]。若有資料需要附加則傳回，沒有則傳回 `None` 或不回傳 [9]。
*   **流程**：統一由 `StrategyPipeline` (strategy_pipeline.py) 集中管理與執行 [8, 9]。
*   **標準範例**：`profile_sync_strategy` 應視為此架構的標準範例，而非特殊例外 [8, 10]。

#### 4.2 Active Strategy (Decision Strategy / 主動決策型)
*   **定義**：根據目前資料判定後，需要執行特定動作 [9, 11]。
*   **實作**：決策時**絕不直接操作 Telegram 或呼叫 Telegram API** [9, 11]。
*   **輸出**：優先回傳正式定義的 `Action` 或其他標準 Action 結構 [11]。

---

### 5. StrategyPipeline：被動觀察流程
*   `strategy_pipeline.py` 負責集中執行需要觀察每筆訊息的 Passive Strategy [8, 9]。
*   **限制**：Pipeline 本身不負責任何遊戲規則 [9]。
*   **新增策略流程**：
    1. 實作 `observe(parsed, record)` 介面 [9]。
    2. 將其實體加入 Pipeline 執行清單 [9]。
    3. 嚴禁修改 Pipeline 本身的控制流程 [9]。

---

### 6. Trigger：特殊的即時反應方式
Trigger 屬於一種特殊的、即時的訊息反應機制，不等於 Strategy 的全部 [11]。

#### 職責與流程
*   **定位**：收到一筆訊號後，立即判斷這筆訊號是否屬於自己，若屬於則產生 Action [11]。
*   **執行介面**：實作 `decide(ctx)`。若傳回 `None` 則表示「這不是我的事情」 [11]。
*   **適用場景**：明確的即時事件、明確的按鈕回應、伺服器訊號出現後的立即處理、不需要大量歷史資料的簡單反應 [11]。

#### Trigger 與 Strategy 的區分標準 [12]
*   **Trigger**：偏向「收到 A 立即做 B」的簡單映射 [12]。
*   **Strategy**：偏向「理解 A，結合 B 與 C 進行計算，最終決定要不要做 D」的深度理解 [12]。
*   **升級原則**：不要為了維持 Trigger 的架構而強行硬塞複雜邏輯 [12]。一旦判斷需要查詢 JSONL、catalog、歷史資料，需要維護狀態、多階段判斷、多訊息組合或計算最佳行動，應立即升級為 Strategy [12]。

---

### 7. Action：Strategy 與執行的標準介面
Action 的目的在於提供「要做什麼」的抽象描述，而非「要怎麼呼叫 Telegram」 [13]，亦非僅是「再包一層 executor」 [12]。

#### 核心原則
*   **不負責遊戲邏輯** [13]：Action 不得判斷王是否該打、該使用哪個技能、是否進入下一回合、點擊哪個按鈕、或是否需要查閱歷史資料 [13]。這些邏輯皆為 Strategy 的責任，Action 只描述執行結果 [13]。
*   **存在價值** [12]：Action 只有在提供以下價值時才值得存在，否則應予以簡化，避免建立大量無意義的 Action mode [12]：
    *   統一不同 Strategy 的回傳格式 [12]。
    *   支援延後執行 [12]。
    *   允許 Dispatcher 決定流程要 `stop` 或 `continue` [12]。
    *   測試時可與實際執行層（Telegram API）進行物理分離 [12]。
    *   為未來可能的多種執行器提供適配能力 [12]。
    *   執行前需要統一進行攔截、記錄或權限判斷 [12]。

---

### 8. Executor：唯一的實際輸出層
`executor.py` 接受上層已經決定好的動作並執行 [14]。「**Executor 不思考，只負責執行。**」 [15]。

#### 負責 (可以做)
*   呼叫 Telegram API、執行實際的訊息送出與按鈕點擊 [14, 15]。
*   實作輸出前的統一延遲機制 [15]。
*   記錄輸出紀錄 [15]。
*   進行 Telegram API 的錯誤處理與例外控管 [15]。

#### 不負責 (不可以做)
*   **禁止** 判斷遊戲狀態 [14]、解析遊戲規則或遊戲訊息 [14, 15]。
*   **禁止** 決定是否應該執行，或決定下一步該做什麼 [14, 15]。

---

### 9. Scheduler：時間管理器
Scheduler 負責時間控制，而不是遊戲策略 [15]。它只知道「時間」，不知道「遊戲語意」 [14, 15]。

#### 負責 (可以做)
*   管理 `delay`、`at`、`repeat`、`interval` [14, 15]。
*   負責 Job 管理、`cancel`、`list` 以及執行 `sequence` [14, 15]。

#### 不負責 (不可以做)
*   **不應成為另一套決策系統** [16, 17]：Scheduler 不得理解或加入「如果...就...」等與遊戲狀態相關的判斷邏輯 [17]。
*   **互動限制**：Strategy 決定「要不要做」，Scheduler 負責「什麼時候做」 [16]。Strategy 不應直接呼叫 Scheduler，而應產生含有時間特徵的 Action，由 Action 執行層統一轉交 Scheduler 處理 [16]。不應在 Strategy 中自行呼叫 `time.sleep` 來繞過 Action / Scheduler [16]。

---

### 10. Reaction Rules：資料驅動的簡單 Strategy
`reaction_rules.py` 的定位是**簡單、資料驅動的無狀態反應規則** [16, 17]。

#### 適用場景
*   簡單的文字命中與固定回應（例如：收到特定文字，回應固定動作） [16, 17]。
*   不需要理解複雜遊戲狀態、歷史資料，亦不需維護跨訊息狀態 [17]。

#### 限制與升級
*   **不應承擔複雜邏輯** [16]：若規則開始需要多條件判斷、跨訊息狀態、歷史 JSONL、catalog、數值計算、多階段決策或複雜例外，應建立正式的 Strategy，嚴禁繼續堆砌於 `reaction_rules.json` 中 [10, 16, 17]。
*   **必須走 Action 輸出** [10]：Reaction 規則不應因歷史原因直接呼叫 Executor，其標準流程應產生 Action 並回傳，與其他 Strategy 共用相同的輸出路徑 [10]。

---

### 11. Runtime State 與 Persistence 的區分
#### Runtime State (暫時狀態)
*   **定位**：保存「程式目前正在等待／進行什麼」的跨訊息短期暫時狀態 [17, 18]。
*   **特徵**：屬於短期資料（存於記憶體或 Redis） [19]，例如「Bot 目前正在等待使用者點擊某個按鈕」 [18]。
*   **限制**：它不是遊戲資料庫，不應取代永久資料 [17, 18]。

#### Persistent Data / Persistence (永久資料)
*   **定位**：用於長期保存的資料（如 JSON, JSONL, catalog） [19]。
*   **特徵**：由對應的 Strategy / 資料層管理 [19]。Strategy 進行 JSONL 讀寫、catalog 讀取、更新 JSON 或分析數據屬於其正常能力，非特殊例外 [10]。

兩者**絕對不要混用** [18]。

---

### 12. Dispatcher 與基礎設施
#### Dispatcher (ActionDispatcher)
*   **職責**：接線與流程控制 [18]。負責將訊息交給適當的決策模組，並控制流程流轉 [18]。
*   **原則**：Dispatcher 負責**「誰來判斷」**，不負責**「怎麼判斷」** [18]。不得逐漸將其寫成「所有遊戲規則的集中地」 [18]。

#### Telegram Client (telegram_client.py)
*   **職責**：Telegram Connection 的共同基礎設施，管理 `client`、`account`、`connection` 與基礎 `Telegram session` [19]。
*   **不負責**：不負責遊戲判斷、Strategy 決策、Reaction Rules 或是 Scheduler [19]。

---

### 13. 依賴關係與 Import 規範
*   **依賴方向**：應保持由高層模組依賴低層模組的方向，避免任何反向依賴或循環依賴 [18, 19]。
*   **共享資料處理**：若多個模組需要共用資料，應將該資料存取與轉換邏輯抽到獨立的 `data/` 或 `shared/` 模組中，嚴禁讓兩個高層模組互相 import [19]。
*   **禁止用 import 繞過架構** [20]：例如，Strategy 為了圖方便而直接 import 寫入 `executor` 並自行執行。此類寫法即使程式運作正常，一律視為架構違規 [20]。

---

### 14. 職責歸屬判斷
當不確定「這段程式應該放哪裡」時，不要看檔名，優先檢視這段程式回答了哪一個問題，並依據下表分類歸屬 [21]：

| 程式回答的問題 | 應歸屬的模組 / 分層 |
| :--- | :--- |
| **這是什麼訊息？** / 這是在解析「這是什麼」嗎？ [21, 22] | **Parser** |
| **固定文字碰到固定動作？** / 只是固定文字 → 固定動作嗎？ [21, 22] | **Reaction Rules** |
| **這個固定訊號要不要回應？** / 收到明確訊號後立即反應嗎？ [21, 22] | **Trigger** |
| **這個內容代表什麼狀態？** / 根據資料現在應該做什麼？ [21] | **Strategy** |
| **只是記錄/同步資料？** / 只是保存、同步或整理資料嗎？ [21, 22] | **Passive Strategy** (StrategyPipeline / Persistence) |
| **什麼時間做？** / 只是決定「何時執行」嗎？ [21, 22] | **Scheduler** |
| **實際怎麼送出去？** / 只是實際送 Telegram 指令嗎？ [21, 22] | **Executor** |
| **現在暫時處於什麼流程？** [21] | **Runtime State** |

---

### 15. 資料讀取慣性與 Loader 規範

#### 15.1 資料讀取不是越少越好
*   同一份資料被多個模組讀取，**不視為問題本身** [23]。
*   **判斷標準**：不是「這份 JSON 被讀了幾次？」，而是「**這次讀取是否為該模組完成自身責任所必要？**」 [23]。例如 `tops.json` 被戰鬥策略、戰鬥狀態、陀螺顯示與 roster loader 同時讀取，完全合理 [23]。

#### 15.2 區分「必要讀取」與「重複讀取」
##### 必要讀取 (合理狀況，不應為了減少讀取強行合併) [23]
*   不同 Strategy 在各自生命週期中，需要當前最新資料（如陀螺數值）進行獨立判斷 [23]。
*   不同流程在不同時間點需要最新狀態 [23]。
*   模組本身具有獨立生命週期，且不能合理依賴另一個模組的內部狀態 [23]。
*   直接讀取能維持模組責任劃分清楚 [23]。

##### 重複讀取 (必須重構的架構問題) [24]
*   同一個模組在同一個事件流程中，重複讀取同一份檔案（例如 A 函式讀一次，B 函式又讀一次） [24]。
*   為了傳遞資料，卻在不相干的兩層分別讀取同一份檔案 [24]。

#### 15.3 Loader 的設計目的
*   Loader 的目的在於**「共通的資料格式處理只維護一份」**，而非「強迫所有人只能透過同一個函式進行實體讀檔」 [24]。
*   **核心原則：共用的是「資料語意與轉換邏輯」，不是強制共用每一次檔案讀取** [3]。
*   若多個模組都需要相同的格式解析與資料轉換（例如：處理 `tops.json` 格式、`roster` 結構、`catalog` 組合等），應優先共用 Loader 處理 [3]；但若模組僅需要獲取非常單純的欄位（如讀取 snapshot），則可直接讀取，不必強行載入完整 Roster Loader [3, 24]。

#### 15.4 是否抽取共用函式之 5 大檢查步驟 [24]
發現有兩處程式讀取相同資料時，應依序檢查：
1.  **是否只是同一份資料被各自使用？**
    *   *是* → 保留各自讀取 [24]。
2.  **是否有重複的格式解析？**
    *   *是* → 考慮抽取 Loader [24]。
3.  **是否有重複的資料轉換？**
    *   *是* → 考慮抽取共用函式 [24]。
4.  **是否只是因為模組需要最新資料，所以各自重新讀取？**
    *   *是* → 通常保留各自讀取 [24]。
5.  **抽象化後，是否反而讓模組依賴了另一個 Strategy 的內部實作？**
    *   *是* → 堅決不抽取 [24]。

---

## 16. 遊戲資料與戰鬥資料規則

本專案的遊戲功能以現有遊戲資料為基礎，不要求每個功能建立完整或獨立的資料模型。

### 16.1 Tops 是戰鬥配置的基準

戰鬥相關功能以目前的 Tops 選擇作為陀螺配置的基準。

* 戰鬥 Strategy 需要知道目前使用哪些陀螺時，應依目前 Tops 選擇取得對應資料。
* 不同戰鬥功能不應各自建立一套「目前使用陀螺」的判斷方式。
* 若 Tops 選擇改變，後續戰鬥判斷應以新的選擇為準。

簡單原則：

> **先確認目前使用的 Tops，再進行戰鬥判斷。**

---

### 16.2 Roster Loader 的用途

`roster_loader` 負責將陀螺相關資料整理成其他程式可以直接使用的形式。

需要目前陀螺配置的功能，優先使用 `roster_loader` 已整理好的資料，不要在各個戰鬥 Strategy 中重新讀取並組合相同的陀螺資料。

但 Loader 不需要提供所有遊戲資料。

> **只整理實際會被使用的資料，不為了完整而完整。**

---

### 16.3 Battle Status 的用途

`battle_status` 負責整理戰鬥判斷需要的資訊。

Battle Status 不需要保存完整的陀螺資料，也不需要計算所有可能的戰鬥數值。

如果戰鬥只需要某項資訊，就只整理該項資訊。

例如：

```text
Tops / Roster
      ↓
Battle Status
      ↓
戰鬥需要的資訊
```

Battle Status 的目的是讓戰鬥 Strategy 容易取得需要的資料，而不是建立完整的遊戲資料模型。

---

### 16.4 Weakness 的判斷方式

目前需要的 weakness 判斷以既有資料表查詢為主。

戰鬥 Strategy 不應各自重新建立 weakness 判斷規則，也不需要為了得到 weakness 而計算不需要的詳細數值差異。

```text
目前戰鬥資料
      ↓
Weakness 查表
      ↓
Weakness 結果
```

如果現有資料表已能直接得到所需結果，就優先查表，不另外建立計算邏輯。

---

### 16.5 資料整理的程度

遊戲資料只整理到目前功能實際需要的程度。

不應因為「未來可能會用到」而提前建立大量資料欄位、完整模型或複雜計算。

新增資料時先確認：

1. 目前功能是否真的需要？
2. 是否已有 Loader、Battle Status 或其他現有資料可以直接使用？
3. 是否只是為了方便某一個 Strategy 而建立重複資料？

如果目前不需要，不必先建立。

---

### 16.6 資料引用原則

不同功能可以各自讀取自己需要的最新資料；資料被讀取多次本身不是問題。

需要避免的是：

* 相同資料格式被不同地方各自重新解析。
* 相同的陀螺資料被不同 Strategy 各自重新整理。
* 不同戰鬥功能各自建立不同的 Tops 選擇邏輯。
* 相同的 weakness 規則在不同地方重複實作。

因此：

> **消除重複的資料整理與判斷邏輯，不要求消除所有資料讀取。**

---

### 16.7 核心原則

本專案的遊戲資料處理以「實際需要」為準：

> **Tops 決定目前使用的陀螺，Roster Loader 提供陀螺資料，Battle Status 整理戰鬥需要的資訊，Weakness 以既有資料表查詢。**

不需要的資料不必整理，不需要的計算不必建立，不同功能也不應各自維護相同的資料判斷。

## 17. Shared / Common Modules：跨程式共用功能

本專案中，若一段程式邏輯會被兩個以上彼此獨立的程式或模組重複使用，
應優先考慮抽取為共用模組，而不是各自複製一份。

### 17.1 共用模組的目的

共用模組的目的不是單純減少程式碼行數，而是確保：

- 相同的資料格式只需要維護一份解析邏輯。
- 相同的通用行為只需要維護一份實作。
- 不同程式對同一資料或功能的理解保持一致。
- 修正共通問題時，可以由單一位置更新。

因此，「可以共用」與「應該共用」並不完全相同。

只有當共用後能維持清楚的責任邊界，且確實代表相同的概念時，
才應抽取為 Shared / Common Module。

### 17.2 什麼適合放入共用模組

適合抽取的內容包括：

- 多個程式都會使用的資料 Loader。
- JSON / JSONL / CSV 等資料格式的共通解析。
- 路徑解析與專案目錄定位。
- 共通的資料轉換函式。
- 與遊戲規則無關的通用工具函式。
- 多個程式需要使用的共通資料結構或型別。
- 多個 Strategy 都需要使用的純計算邏輯。

### 17.3 不應為了共用而共用

以下情況不應強行抽取：

- 只有一個模組使用。
- 雖然程式碼相似，但實際語意不同。
- 抽取後必須知道呼叫端的特殊狀態才能運作。
- 共用模組因此開始理解特定 Strategy 的遊戲規則。
- 為了共用而產生過度複雜的參數或 callback。
- 抽取後反而造成高層模組依賴彼此的內部實作。

**程式碼相似，不代表責任相同。**

### 17.4 共用模組的依賴原則

Shared / Common Module 應保持低耦合。

共用模組可以被：

- Monitor
- Parser
- Strategy
- Scheduler
- Executor
- 工具程式

等多個模組引用。

但共用模組不應反過來依賴特定的高層 Strategy、Executor 或 Scheduler。

例如：

    Strategy A ─┐
    Strategy B ─┼→ shared utility
    Tool C ─────┘

是合理的。

而：

    shared utility → Strategy A

則代表共用模組開始依賴特定業務邏輯，應重新檢查架構。

### 17.5 新程式的規範

新增程式時，如果需要某項功能：

1. 先搜尋現有 Shared / Common Module。
2. 若已有相同概念，優先直接引用。
3. 若沒有，但判斷未來會被多個程式使用，可以考慮建立共用模組。
4. 不應直接複製既有程式碼後各自維護。
5. 若選擇不共用，應確認兩段程式的責任或語意確實不同。

### 17.6 共用模組不是「萬用垃圾桶」

不得因為某個函式「可能以後會用到」，
就將其全部塞入單一 `utils.py`。

共用模組仍應依功能與責任分類。

例如：

- `data_loader.py` → 資料載入與格式轉換
- `path_utils.py` → 路徑處理
- `format_utils.py` → 共通格式化
- `game_calculations.py` → 純遊戲計算
- `telegram_client.py` → Telegram 基礎連線

而不是建立一個沒有明確責任的巨大 `utils.py`。

### 17.7 共用模組的判斷原則

判斷是否應抽取共用模組時，優先問：

> 「這是不是同一個概念的單一實作？」

而不是：

> 「這兩段程式碼是不是長得很像？」

如果答案是前者，應優先考慮共用。

如果只是後者，則應先確認是否真的具有相同責任。

---

### 18. 開發、重構與特例處理原則

#### 18.1 新增 Strategy 的標準流程
*   Strategy 本身**嚴禁直接送出 Telegram 訊息** [25]。
*   **標準模式**：
    1.  Strategy 根據資料判斷，產生並回傳 `Action` [22]。
    2.  `Dispatcher` 接收 `Action` 並進行流程控制 [18, 22]。
    3.  `Dispatcher` 將其派發給 `Executor` 執行 [22]。
    *   *若需要延遲執行*：Strategy 應產生帶有延遲資訊的 Action，由 Dispatcher 轉交 `Scheduler` 建立 Job，而非由 Strategy 自行排程或使用 `sleep` [16, 22]。

#### 18.2 複雜功能的拆分模式
*   當 Strategy 變得過於龐大時，**不要**將程式塞入 Dispatcher [25]。
*   應優先將其拆分為多個子 Strategy（或子模組），但**僅有當拆分能讓責任更清楚時才進行拆分** [25]。不要為了維持「檔案數量看起來漂亮」而進行過度拆分 [25]。

#### 18.3 特例處理與技術債
*   歷史遺留程式可以暫時存在架構例外，但特例必須 [22, 25]：
    1.  明確知道其為什麼存在 [25]。
    2.  **不應成為新增功能時的模仿範例** [22, 25]。
    3.  後續重構時優先消除 [25]。
*   目前尤其需要注意的歷史包袱：`sakura_strategy`、`world_boss_strategy`、舊版 `reaction_rules`、以及直接呼叫 executor 的舊 Strategy [26]。新功能開發一律不得複製這些歷史寫法 [25, 26]。

#### 18.4 World Boss 模組重寫規範
在重新設計 World Boss 模組之前，必須遵循以下要求 [26]：
1.  不再增加新的特殊例外 [26]。
2.  不直接模仿現有的 `world_boss_strategy` [26]。
3.  **只繼承功能需求，不繼承舊架構的寫法** [27]。重寫時不需要相容舊有的耦合路徑，應直接按照目前的標準分層架構重新建立 [27]。
4.  先決定它屬於哪一種 Strategy（Active 還是 Passive） [26]。
5.  判斷「資料建立」與「行動判斷」是否需要拆開 [26]。
6.  明確定義其輸入資料與輸出 Action 介面 [26]。
7.  將複雜判斷規則獨立成可測試的函式或子模組 [26]。
8.  **禁止**讓 Scheduler 或者是 Executor 開始理解與介入世界王的規則 [26]。
*   「**複雜的是規則，不應該是架構。**」 [26]。

---

### 19. 三大黃金守則
如果未來在開發時只能記住三條規則，請務必嚴守這三條 [27]：
*   **① Strategy 決定「要不要做什麼」** [27]
*   **② Action 描述「要做什麼」** [27]
*   **③ Executor / Scheduler 決定「怎麼做、什麼時候做」** [27]

---

### 20. 最終架構目標
這套架構的目標並非強求所有程式都「長得一模一樣」 [27]。
其最終目標是：
*   讓每一段程式的責任可以用一句話說清楚，且新增功能時不需要重新發明架構 [21]。
*   讓每個模組都能清晰回答：**「我為什麼在這一層？」** [27]。如果無法回答，就應該重新檢查模組邊界 [27]。